apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-SERVICE_PLACEHOLDER
  namespace: default
spec:
  mode: one
  selector:
    labelSelectors:
      app: SERVICE_PLACEHOLDER
  stressors:
    cpu:
      workers: 4
      load: 90
  duration: '60s'
